<local:VerticalVirtualizingPanel x:Class="ArtWiz.View.Widgets.CodeBlock.CodeBlock"
								 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
								 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
								 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
								 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
								 xmlns:local="clr-namespace:ArtWiz.View.Widgets.CodeBlock"
								 xmlns:def="clr-namespace:ArtWiz"
								 mc:Ignorable="d"
								 FontFamily="Cascadia Code"
								 FontSize="14"
								 d:DesignHeight="450"
								 d:DesignWidth="800">
	<local:VerticalVirtualizingPanel.Resources>
		<SolidColorBrush x:Key="TextBox.MouseOver.Border"
						 Color="#FF7EB4EA" />
		<SolidColorBrush x:Key="TextBox.Focus.Border"
						 Color="#FF569DE5" />
		<ControlTemplate x:Key="TextBoxTemplate1"
						 TargetType="{x:Type TextBoxBase}">
			<Border x:Name="border"
					Background="{TemplateBinding Background}"
					BorderBrush="{TemplateBinding BorderBrush}"
					BorderThickness="{TemplateBinding BorderThickness}"
					SnapsToDevicePixels="True">
				<ScrollViewer x:Name="PART_ContentHost"
							  Focusable="false"
							  HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}"
							  VerticalScrollBarVisibility="Hidden">
				</ScrollViewer>
			</Border>
			<ControlTemplate.Triggers>
				<Trigger Property="IsEnabled"
						 Value="false">
					<Setter Property="Opacity"
							TargetName="border"
							Value="0.56" />
				</Trigger>
				<Trigger Property="IsMouseOver"
						 Value="true">
					<Setter Property="BorderBrush"
							TargetName="border"
							Value="{StaticResource TextBox.MouseOver.Border}" />
				</Trigger>
				<Trigger Property="IsKeyboardFocused"
						 Value="true">
					<Setter Property="BorderBrush"
							TargetName="border"
							Value="{StaticResource TextBox.Focus.Border}" />
				</Trigger>
			</ControlTemplate.Triggers>
		</ControlTemplate>
	</local:VerticalVirtualizingPanel.Resources>
	<local:VerticalVirtualizingPanel.ItemTemplate>
		<DataTemplate>
			<Border d:DataContext="{d:DesignInstance Type=local:LineNumberViewModel, IsDesignTimeCreatable=True}"
					BorderBrush="Gray"
					Background="Transparent"
					Height="{Binding LineHeight}"
					BorderThickness="0,0,0,0">
				<TextBlock Text="{Binding LineIndex}"
						   FontSize="13"
						   VerticalAlignment="Center"
						   HorizontalAlignment="Right"
						   Foreground="{StaticResource ResourceKey={x:Static def:Definitions.TextForegroundBrushLevel0_5}}" />
			</Border>
		</DataTemplate>
	</local:VerticalVirtualizingPanel.ItemTemplate>

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="0" />
			<ColumnDefinition Width="35" />
			<ColumnDefinition Width="20" />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="10"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<Canvas Background="{StaticResource ResourceKey={x:Static def:Definitions.PanelBackgroundLevel0_1}}"
				Grid.Column="1"
				Grid.Row="1"
				x:Name="MainCanvasContainer">
			<Canvas x:Name="ContentCanvasContainer">

			</Canvas>
		</Canvas>
		<Rectangle Grid.Row="0"
				   Grid.Column="3"
				   Fill="{StaticResource ResourceKey={x:Static def:Definitions.PanelBackgroundLevel0}}"/>
		<Border Grid.Column="2"
				Grid.RowSpan="2"
				BorderThickness="0,0,1,0"
				Background="{StaticResource ResourceKey={x:Static def:Definitions.PanelBackgroundLevel0_1}}"
				BorderBrush="{StaticResource ResourceKey={x:Static def:Definitions.PanelBackgroundLevel0_10}}"/>
		<TextBox Template="{DynamicResource TextBoxTemplate1}"
				 Name="ContentTextBox"
				 Grid.Row="1"
				 Background="{StaticResource ResourceKey={x:Static def:Definitions.PanelBackgroundLevel0}}"
				 Grid.Column="3"
				 SelectionBrush="{StaticResource ResourceKey={x:Static def:Definitions.ButtonAndIconBrushLevel0}}"
				 Foreground="{StaticResource ResourceKey={x:Static def:Definitions.TextForegroundBrushLevel0}}"
				 SelectionChanged="ContentTextBox_SelectionChanged"
				 AcceptsReturn="True"
				 AcceptsTab="True"
				 BorderThickness="0"
				 VerticalScrollBarVisibility="Hidden"
				 HorizontalScrollBarVisibility="Hidden"
				 TextChanged="ContentTextBox_TextChanged"
				 PreviewMouseWheel="ContentTextBox_PreviewMouseWheel" />

	</Grid>
</local:VerticalVirtualizingPanel>
